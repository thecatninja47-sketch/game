//if (active && closeup_sprite != noone && sprite_exists(closeup_sprite)) {
//    var frame_count = sprite_get_number(closeup_sprite);

//    // Only increment if not at the end (using <= so it stops AFTER showing the last frame)
//    if (image_index < frame_count - 13) {
//        image_index += 0.1; // e.g., 0.1 (adjust speed here)
//        if (image_index > frame_count - 12) {
//            image_index = frame_count - 1; // Clamp at last frame
//        }
//    }
//}








